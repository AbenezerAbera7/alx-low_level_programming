#include <stdio.h>

/**
 * main - prints the phrase with proper grammer,
 * but the outcome is a piece of art,
 * Return: 0 if it's correct, non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
